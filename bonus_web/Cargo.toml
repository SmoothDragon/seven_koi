[package]
name = "koi_bonus"
version = "0.1.0"
edition = "2021"
description = "Koi — Kickstarter digital bonus: Rust compiled to WebAssembly."
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
